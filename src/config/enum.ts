

export enum Action {
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}

export enum AdTypes {
  special = 'special',
  default = 'default',
  poster = 'poster'
}
export enum AdType {
  sell = 'sell',
  rent = 'rent',
}